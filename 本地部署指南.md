# A股每日投资分析报告 - 本地部署指南

## 📦 安装步骤

### 1. 解压项目
将 `a-stock-daily-report.zip` 解压到任意目录，例如：
- Windows: `D:\a-stock-daily-report\`
- Mac: `~/a-stock-daily-report/`

### 2. 安装 Python
如果还没装 Python，去 [python.org](https://python.org) 下载 3.8+ 版本安装。

### 3. 安装依赖
打开终端/命令行，进入项目目录：
```bash
pip install requests
```

### 4. 测试运行
- **Windows**: 双击 `run_windows.bat`
- **Mac/Linux**: 终端执行 `./run_mac.sh`

报告会生成在项目目录下：`A股投资分析报告_2026-07-21.md`

---

## ⏰ 设置每日自动运行

### Windows 任务计划

1. 打开「任务计划程序」（开始菜单搜索）
2. 点击「创建基本任务」
3. 名称填：`A股每日报告`
4. 触发器：每天，时间设为 **20:00**
5. 操作：启动程序
   - 程序路径：`D:\a-stock-daily-report\run_windows.bat`
   - 起始位置：`D:\a-stock-daily-report\`
6. 完成

### Mac crontab

打开终端，输入：
```bash
crontab -e
```
添加一行（每天 20:00 运行）：
```
0 20 * * 1-5 cd ~/a-stock-daily-report && python3 report.py >> ~/a-stock-daily-report/cron.log 2>&1
```
保存退出。

---

## 📂 项目结构

```
a-stock-daily-report/
├── report.py              # 核心报告引擎
├── requirements.txt       # Python 依赖
├── run_windows.bat        # Windows 启动脚本
├── run_mac.sh             # Mac/Linux 启动脚本
├── .github/workflows/     # GitHub Actions（可选）
└── README.md              # 项目说明
```

## 🔧 自定义

编辑 `report.py` 中的 `STOCK_CANDIDATES` 列表可以增减候选股票池。
